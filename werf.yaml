# werf.yaml
project: montalabs
configVersion: 1

build:
  platform: [ linux/amd64 ]

---
image: montalabs
dockerfile: Dockerfile
shell:
  beforeCommand:
    - export WERF_REPO=harbor.mymemorylog.com/test